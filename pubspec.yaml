name: seikatsu_planner
description: Complete Digital Planner System

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # NETWORK & API
  http: ^1.1.0
  dio: ^5.3.2
  
  # STATE MANAGEMENT
  provider: ^6.1.1
  get: ^4.6.6
  
  # LOCAL STORAGE
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  
  # UI COMPONENTS
  google_fonts: ^6.1.0
  flutter_spinkit: ^5.2.0
  fl_chart: ^0.65.0
  calendar_timeline: ^1.1.2
  table_calendar: ^3.0.9
  
  # UTILITIES
  intl: ^0.19.0
  uuid: ^4.2.1
  image_picker: ^1.0.4
  file_picker: ^6.1.1
  
  # ANIMATIONS
  lottie: ^2.7.0
  animate_do: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
   